#include <iostream>
#include <algorithm>
#include <vector>
#include <string>

struct inputImage {
    std::string path;
    std::vector<int> buffer;
    size_t width;
    size_t height;
    size_t depth;
};






int main(int argc, char* argv[]) {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
